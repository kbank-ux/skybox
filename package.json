{
  "name": "skybox",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.13.0",
    "@parcel/transformer-sass": "^2.13.0",
    "@parcel/transformer-webmanifest": "^2.13.0",
    "esbuild": "^0.25.0",
    "eslint": "^9.15.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.2.1",
    "gh-pages": "^6.2.0",
    "parcel": "^2.13.0",
    "path-browserify": "^1.0.0",
    "prettier": "^3.3.3",
    "process": "^0.11.10",
    "svgo": "^3",
    "tsup": "^8.3.5",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.15.0"
  },
  "scripts": {
    "lint": "eslint --fix src/**/*.ts",
    "dev": "yarn build && parcel index.html",
    "build": "parcel build index.html --public-url /skybox/",
    "deploy": "yarn build && gh-pages -d dist"
  },
  "dependencies": {
    "box2d-wasm": "^7.0.0"
  }
}
